# Aby uruchomić program trzeba:

* Bash :

python -m venv .venv

* Bash :

source .venv/bin/activate

* Bash : 

pip install fastapi uvicorn openai-whisper python-multipart

* Bash :

python3 Backend.py

* Po wykorzystaniu tego kodu wyświetla się link do uruchomienia programu w przeglądarce
